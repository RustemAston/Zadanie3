SELECT * FROM users JOIN adverts ON users.id = adverts.user_id ORDER BY users.id;

SELECT * FROM users JOIN user_society ON users.id=user_society.user_id
                JOIN societies ON user_society.society_id=societies.id;

SELECT * FROM users u LEFT OUTER JOIN adverts a ON a.user_id = u.id;
